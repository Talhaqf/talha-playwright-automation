Playwright sample project generated by assistant.

Folder structure:
- tests/smoke
- tests/regression
- tests/api
- pages/ (POM)
- helpers/

Run locally:
1. npm install
2. npx playwright install
3. npm run test:smoke

CI:
- Copy .github/workflows/ci.yml to your repo, set BASE_URL in secrets.
